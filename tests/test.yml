# test file
---
- hosts: localhost
  connection: local
  become: true
  pre_tasks:
    - include: pre.yml
  roles:
    - ../../
  vars:
    nagios_client_authorized_keys:
      - src: "{{ playbook_dir }}/../files/id_rsa.pub"
